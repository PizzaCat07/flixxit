import React, { useEffect } from "react";
import Header from "../../shared/components/Header";

const Title = ({ id, type }) => {
  useEffect(() => {}, [id]);

  return (
    <>
      <div>Title</div>
    </>
  );
};

export default Title;
